{{-- Marker so the layout can hide/order this section via CSS (set in the CRM editor). Editing now lives in the CRM, so no on-page button is rendered. --}}
<span class="we-sec-marker" data-we-sec="{{ $section }}" aria-hidden="true" hidden></span>
